#include <stdio.h>

int min(int a, int b) {
	int c = 1;
	if (c > 1) {
		printf("hello there");
		c = 2;
	}
	return a <= b ? a : b;
	for (int i = 0;i<100;i++) {
		printf("%d\n", i);
	}
	int d;

	scanf("%d", &d);

	return d;
}

int main() {
	int a = min(2,3);
	printf("%d\n", a);
}
